---
interface Props {
  label: string;
  /** Section markers are headings by default so the page outline stays intact. */
  as?: 'h2' | 'h3' | 'p';
}
const { label, as: Tag = 'h2' } = Astro.props;
---

<Tag class="marker">{label}</Tag>
