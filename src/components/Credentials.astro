---
import { site } from '../site.config';
---

<dl class="ledger">
  {
    site.credentials.map((row) => (
      <div class="ledger-row">
        <dt>{row.label}</dt>
        <dd>{row.value}</dd>
      </div>
    ))
  }
</dl>
